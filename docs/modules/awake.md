# awake — Keep awake

Prevent sleep / display off without changing power settings.

## Syntax

```
awake    → toggle on / off
```

## Behavior

| State | Effect |
|-------|--------|
| Off → On | System + display stay awake; sticky toast |
| On → Off | Normal sleep timers resume |

No duration / arguments for now.
